server:
  port: 8085


spring:
  application:
    name: pgsainia-provider-pay-consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
