server:
  port: 82


spring:
  application:
    name: pgsainia-consumer-order-consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
